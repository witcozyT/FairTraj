# Checkpoints

Model checkpoints are not included by default.

Generated DWGAT checkpoints and other model weights should be saved locally, for example:

```text
outputs/fairtraj_core/dwgat.pth
```

Before public release, decide whether trained FairTraj weights can be redistributed and document their download URL here.
